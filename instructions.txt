The model was created with train_model.py
steps to run the frontend:
1. cd musicmood-frontend
2. npm install
3. npm run dev


steps to run the ipynb:
1. pip install -r requirements.txt
2. run the ipynb file in jupyter notebook